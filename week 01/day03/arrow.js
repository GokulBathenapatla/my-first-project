//function add(a, b) { ///it is a normal fun
//    return a + b;// a funtion is a block of that perform a specific task
//}

//console.log(add(10, 20));




//const add = (a, b) => { //this is a arrow function.
  //  return a + b;// we represent arrow fun as =>
//};//we use arrow function be when we compare the the normal function the arrow fun is more shorter and convient
//console.log(10 + 5);// we will use more in react the arrow functions.

//in this method we will right in react 
const numbers = [10, 20, 30];

const result = numbers.map(num => num * 2);//it will take every element in an array and it will create a new array.

console.log(result);