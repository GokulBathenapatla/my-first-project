function greet(name = "gokul") { // default means if the user didn't provide any value  means it will this default value in output.
    console.log(`Hello ${name}`);
}
greet();// in parameters itself we can give the values through we can we call the method we will the ouptut
// in para there we can give one value and in greet method when we give a value it will print that value  